{
  "name": "@trema/docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cp ../server/openapi.json ./openapi.json && blume dev",
    "lint:prose": "vale docs",
    "sync:spec": "cp ../server/openapi.json ./openapi.json",
    "build": "cp ../server/openapi.json ./openapi.json && blume build",
    "stage:assets": "rm -rf .cloudflare && mkdir -p .cloudflare/docs && cp -R dist/. .cloudflare/docs/ && mv .cloudflare/docs/_headers .cloudflare/_headers",
    "deploy": "cp ../server/openapi.json ./openapi.json && blume build && pnpm run stage:assets && wrangler deploy"
  },
  "dependencies": {
    "blume": "^1.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
